[tools]
# Latest stable pair. Elixir prebuilt binaries only go up to otp-28 (no otp-29
# build yet), so we stay on the newest OTP 28 rather than jumping to Erlang 29.
erlang = "28.5.0.3"
elixir = "1.20.2-otp-28"
