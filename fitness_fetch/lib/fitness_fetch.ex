defmodule FitnessFetch do
  @moduledoc """
  Pulls training data from external services into a shape ready for the weekly
  logs.

  See `FitnessFetch.Strava` for the Strava client and the `strava.fetch` /
  `strava.auth` mix tasks for the manual entry points.
  """
end
